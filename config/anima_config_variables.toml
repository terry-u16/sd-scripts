max_train_steps = 2500
learning_rate = 2e-5
gradient_checkpointing = true
gradient_accumulation_steps = 4
train_batch_size = 1

# Character LoRA
#network_dim = 32
#network_alpha = 32

# Style LoRA example
network_dim = 64
network_alpha = 64

#metadata_description = "recommended tags: hoge"
output_name = "hoge_anima_v1.0"

# Optional sampling / checkpoint cadence
save_every_n_steps = 500
sample_every_n_steps = 100

# Qwen3/T5 token lengths
qwen3_max_token_length = 512
t5_max_token_length = 512
